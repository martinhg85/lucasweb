---
import { site } from "../data/site";
import { Icon } from "astro-icon/components";

interface Props {
  items?: readonly string[];
  label?: string;
}

const { items = site.regulations, label = "Trabajo bajo el marco de" } =
  Astro.props;

const parsed = items.map((raw) => {
  const [name, ...rest] = raw.split(" — ");
  return {
    name: name.trim(),
    description: rest.join(" — ").trim() || undefined,
  };
});
---

<aside
  class="relative bg-gradient-to-br from-ink-900 to-ink-700 text-white overflow-hidden"
  aria-label="Marco normativo"
>
  <span
    class="absolute -top-32 -right-32 w-[28rem] h-[28rem] rounded-full bg-sky-500/15 blur-3xl pointer-events-none"
    aria-hidden="true"
  ></span>

  <div class="container-tight py-16 sm:py-20 relative reveal">
    <div class="flex items-center justify-center gap-3 mb-6">
      <Icon
        name="ph:shield-check-bold"
        class="w-6 h-6 text-sky-400"
        aria-hidden="true"
      />
      <p class="text-[11px] font-bold tracking-[0.22em] uppercase text-sky-300">
        {label}
      </p>
    </div>

    <div class="mx-auto max-w-md border-t border-white/15 mb-12"></div>

    <ul
      class="grid grid-cols-2 md:grid-cols-3 gap-x-8 gap-y-10 max-w-5xl mx-auto"
    >
      {parsed.map((r) => (
        <li class="text-center">
          <p class="font-display text-sm sm:text-base font-bold tracking-tight text-white leading-tight">
            {r.name}
          </p>
          {r.description && (
            <p class="mt-2 text-xs text-white/70 leading-relaxed">
              {r.description}
            </p>
          )}
        </li>
      ))}
    </ul>
  </div>
</aside>
