---
import { site, services, mailtoUrl, whatsappUrl } from "../data/site";
import { Icon } from "astro-icon/components";
const year = new Date().getFullYear();
---

<footer class="bg-ink-900 text-white">
  <div class="container-tight py-16 sm:py-20">
    <div class="grid gap-12 md:grid-cols-[1.4fr_1fr_1fr]">
      <div>
        <div class="flex items-center gap-3">
          <span
            class="grid place-items-center w-12 h-12 rounded-2xl bg-gradient-to-br from-sky-500 to-sky-600 text-white shadow-md shadow-sky-500/30"
          >
            <Icon name="ph:hard-hat-bold" class="w-6 h-6" />
          </span>
          <p class="font-bold text-xl tracking-tight">Lucas Contreras</p>
        </div>
        <p class="mt-5 text-white/70 text-sm max-w-sm leading-relaxed">
          {site.tagline}. Atención personal en {site.coverage.extended}.
        </p>
        <p class="mt-3 text-white/50 text-xs">
          {site.professional.matriculas.map((m) => `${m.colegio} ${m.numero}`).join(" · ")}
        </p>
      </div>

      <div>
        <p class="text-xs font-bold tracking-widest uppercase text-sky-400 mb-5">
          Servicios
        </p>
        <ul class="space-y-3 text-sm text-white/80">
          {services.map((s) => (
            <li>
              <a
                href={`/servicios/${s.slug}`}
                class="hover:text-sky-300 transition-colors"
              >
                {s.title}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <div>
        <p class="text-xs font-bold tracking-widest uppercase text-sky-400 mb-5">
          Contacto
        </p>
        <ul class="space-y-3 text-sm text-white/80">
          <li>
            <a
              href={whatsappUrl()}
              target="_blank"
              rel="noopener"
              class="hover:text-sky-300 transition-colors"
            >
              {site.contact.phoneDisplay}
            </a>
          </li>
          <li>
            <a
              href={mailtoUrl()}
              class="hover:text-sky-300 transition-colors break-all"
            >
              {site.contact.email}
            </a>
          </li>
          <li class="text-white/50">{site.coverage.extended}</li>
        </ul>
      </div>
    </div>
  </div>

  <div class="border-t border-white/10">
    <div
      class="container-tight py-6 flex flex-col sm:flex-row items-center justify-between gap-2 text-xs text-white/50"
    >
      <p>© {year} {site.professional.fullName}. Todos los derechos reservados.</p>
      <p>Buenos Aires, Argentina</p>
    </div>
  </div>
</footer>
